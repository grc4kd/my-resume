import { ExperienceDetailPipe } from './experience-detail-pipe.pipe';

describe('ExperienceDetailPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ExperienceDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
